Options -Indexes

RewriteEngine On
RewriteBase /

# Serve 404.php for any path that isn't a real file or directory
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ /404.php [L]
